name: Test Action on PR

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  lint-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name:
        run: |
          echo "Hello?"
